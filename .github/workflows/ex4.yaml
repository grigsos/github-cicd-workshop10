name: Build and Deploy-ex4

on:
  pull_request:
    branches: [master]

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout github repository
        uses: actions/checkout@v4

      - name: Simulate building
        run: sleep 5

      - name: Finish building
        run: echo "Build completed successfully"

  deploy:
    runs-on: ubuntu-latest
    needs: [build-and-push]
    strategy:
      fail-fast: true
      max-parallel: 1
      matrix:
        environment: [ 'dev', 'int' ]
        version: [ 'v1', 'v2' ]
    environment: ${{ matrix.environment }}-${{ matrix.version }}
    steps:
      - name: Checkout github repository
        uses: actions/checkout@v4

      - name: Deploying to environment ${{ matrix.environment }} with version ${{ matrix.version }}
        run: echo "Deploying to environment ${{ matrix.environment }} with version ${{ matrix.version }}"

      - name: Simulate deployment
        run: sleep 5

      - name: Finish deployment
        run: echo "Deployment completed successfully for environment ${{ matrix.environment }} with version ${{ matrix.version }}"
